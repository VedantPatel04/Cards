# Cards local / compose environment template.
# Copy to `.env` at the repo root if you want compose to read root-level overrides.
# The web service also loads `backend/.env` when present.

# Used by docker-compose for the Postgres service and the web DATABASE_URL.
POSTGRES_USER=cards
POSTGRES_PASSWORD=cards
POSTGRES_DB=cards_db
